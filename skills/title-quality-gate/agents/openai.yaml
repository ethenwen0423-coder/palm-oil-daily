interface:
  display_name: "Title Quality Gate"
  short_description: "Inspect research title quality only"
  default_prompt: "Use $title-quality-gate to inspect an existing research title set and return pass or failed checks without generating replacement titles."

policy:
  allow_implicit_invocation: true
